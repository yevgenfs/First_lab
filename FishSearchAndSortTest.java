package ua.lviv.IoT.fish;

import static org.junit.jupiter.api.Assertions.*;
import static org.junit.jupiter.api.Assertions.assertEquals;
import java.util.ArrayList;
import java.util.List;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

/*class FishSearchAndSortTest {

  @BeforeEach
  void setUp() throws Exception {
    FishProducts fish = new FishProducts();
    List<FishProducts> fishList =  new ArrayList<FishProducts>();
    fishList.add(new  FishProducts(KindOfFish.FRESHFISH,15,Import.IMPORT,true,"England",
        QualityOfFish.WELLQUALITY,"a",54));
    fishList.add(new  FishProducts(KindOfFish.FRESHFISH,20,Import.IMPORT,true,"England",
         QualityOfFish.WORSTQUALITY,"b",48));
    fishList.add(new  FishProducts(KindOfFish.FRESHFISH,30,Import.IMPORT,true,"England",
         QualityOfFish.MIDLEQUALITY,"c",50));
    fishList.add(new  FishProducts(KindOfFish.COLDFISH,20,Import.IMPORT,true,"England",
         QualityOfFish.WELLQUALITY,"ras",53));
    fishList.add(new  FishProducts(KindOfFish.COLDFISH,20,Import.IMPORT,true,"England",
         QualityOfFish.WELLQUALITY,"ras",52));
  }

  @Test
  void testWeightSort(List<FishProducts> fishList,boolean incrise) {
    fishList.get(0).weightSort(fishList,false);

    assertEquals(54, fishList.weightSort(fishList.get(0).getWeight()));
    assertEquals(53, fishList.testWeightSort(fishList.get(1).getWeight()));
    assertEquals(52, fishList.testWeightSort(fishList.get(2).getWeight()));
    assertEquals(50, fishList.testWeightSort(fishList.get(3).getWeight()));
    assertEquals(48, fishList.testWeightSort(fishList.get(4).getWeight()));
    
    
  }

}*/
